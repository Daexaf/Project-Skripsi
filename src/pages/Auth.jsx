import React, { Fragment } from "react";
import AuthPages from "../components/auth/auth";

const Auth = () => {
  return (
    <Fragment>
      <AuthPages />
    </Fragment>
  );
};

export default Auth;
